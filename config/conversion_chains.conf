# Conversion Chains Configuration
# Format: source_format → intermediate_format → target_format
# Use when direct conversion is not available or suboptimal

# Document format chains
doc → docx → pdf
doc → docx → odt
xls → xlsx → csv
xls → xlsx → ods
ppt → pptx → pdf
ppt → pptx → odp

# Advanced document conversions
pdf → png → svg
pdf → txt → md
docx → html → epub
odt → html → pdf

# Image format chains
gif → png → jpg
bmp → png → webp
tga → png → jpg
ico → png → svg
wmf → svg → png
emf → svg → pdf

# Vector to raster chains
svg → png → jpg
svg → pdf → png
eps → pdf → png
ps → pdf → jpg

# Audio format chains
wma → wav → mp3
m4a → wav → flac
au → wav → ogg
aiff → wav → mp3

# Video format chains
wmv → avi → mp4
3gp → mp4 → webm
flv → mp4 → mkv
m4v → mp4 → avi

# Specialized chains for quality preservation
tiff → png → webp
raw → tiff → jpg
svg → eps → pdf
latex → pdf → html

# Multi-step document processing
docx → odt → html → epub
xlsx → csv → json → yaml
pptx → pdf → png → svg

# Archive and compression chains
zip → extract → convert → archive
rar → extract → convert → zip

# Complex multimedia chains
mkv → mp4 → webm → gif
mp4 → frames → process → video
audio → spectrogram → image → vector

# Cross-platform compatibility chains
pages → docx → pdf
numbers → xlsx → csv
keynote → pptx → pdf

# Legacy format support
wp → rtf → docx
123 → xls → xlsx
pcx → bmp → png

# Scientific document chains
tex → pdf → html
bib → json → yaml
gnuplot → eps → svg

# Web format optimization chains
png → webp → avif
jpg → webp → heic
svg → optimized_svg → compressed

# Accessibility chains
pdf → html → accessible_html
docx → odt → html → epub
image → ocr_text → accessible_format
